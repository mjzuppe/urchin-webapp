type Entries = {
  id: string;
  updatedAt: number;
  solanaAddress: string;
  arweaveAddress: string;
  title: string;
  // meta description
  // hero image
  // altText
  // post (rich text)
};

export type { Entries };
