type File = {
  // name: string;
  // size: number;
  // type: string;
  // lastModified: number;
  // lastModifiedDate: Date;
  base64: string;
};

type Files = Files[];

export type { File, Files };
